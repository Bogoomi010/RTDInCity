Original prompt: 현재 공격모션, 이동모션, 이미지를 각 캐릭터 문서를 보고 이미지를 새로 뽑아 개선하라. 더 많은 모션의 스프라이트, 서로 일정한 비율 유지, 퀄리티 있는 버스트, 위젯별로 적당한 사이즈로 적용을 위한 각각의 에셋.

## 2026-07-10

- 희귀 등급 6종(`dv4`, `pc4`, `sn4`, `dr4`, `dm4`, `tf4`) 마스터/공격/이동 이미지를 문서 기준으로 재생성했다.
- 공격 8프레임, 정면·후면·측면 이동 각 8프레임으로 확장했다.
- 런타임 프레임 256x256, 캐릭터 높이 180px, 기준선 y=244로 정규화했다.
- 버스트 512px, 패널 128px, 작은 위젯 48px 에셋을 분리했다.
- 긴 총열(`sn4`)과 요원+드론(`dr4`) 셀 간섭을 별도 넓은 원본으로 해결했다.
- `yarn build` 성공. 최종 42개 에셋과 총 192개 모션 프레임의 크기·알파·기준선을 검사했다.
- 실제 브라우저에서 타이틀 → 덱 선택 → 게임 씬 전환 및 새 에셋 선로드 시 콘솔 오류가 없음을 확인했다.
- 전설 등급 6종(`dv5`, `pc5`, `sn5`, `dr5`, `dm5`, `tf5`)의 마스터·8프레임 공격·3방향 8프레임 이동·512/128/48px UI 에셋을 생성하고 등록했다.
- 전설 공격은 긴 무기와 금빛 이펙트를 위해 2x4 넓은 원본을 사용하고, 생성 격자선과 7포즈 이동 원본을 빌드 단계에서 안전하게 보정했다.
- `yarn build` 성공. 전설 최종 42개 에셋과 총 192개 모션 프레임의 크기·알파·기준선·크로마 잔여 픽셀을 검사했다.
- 실제 브라우저에서 타이틀 → 대원 선택 → 전투 화면 전환을 확인했고 콘솔 경고·오류가 없었다.
- 초월 등급 6종(`t1`~`t6`)의 문서 기반 마스터·8프레임 공격·3방향 8프레임 이동 원본을 imagegen 내장 도구로 생성했다.
- 청록 에너지를 보존하기 위해 마젠타 크로마키를 사용하고, 256px 모션·512/128/48px UI 에셋으로 정규화했다.
- 방향별 7포즈로 생성된 `t2`, `t5` 이동은 첫 자세를 복귀 프레임으로 추가했고, `t2` 버스트의 인접 포즈 혼입을 전용 절단 비율로 제거했다.
- `yarn build` 성공. 초월 최종 42개 에셋과 총 192개 모션 프레임의 크기·알파·기준선·복귀 루프·마젠타 잔여 픽셀을 검사했다.
- Playwright 클라이언트를 실행하고 실제 브라우저에서 타이틀 → 대원 선택 → 전투 화면 전환을 확인했으며 콘솔 경고·오류가 없었다.
- 흔함 계열 6종(`dv1`, `pc1`, `sn1`, `dr1`, `dm1`, `tf1`)의 마스터·8프레임 공격·3방향 8프레임 이동을 문서 기준으로 새로 생성했다.
- 캐릭터 일관성 검수에서 헬멧이 빠진 `dv1` 이동, 모자가 빠진 `tf1` 이동, 본체가 빠진 `pc1` 공격을 마스터 참조 재생성으로 교정했다.
- 흔함 계열 런타임을 256px 모션·512/128/48px UI 규격으로 전환하고 구형 4프레임 생성 공격 원본을 교체했다.
- `yarn build` 성공. 흔함 계열 최종 42개 에셋과 총 192개 모션 프레임의 크기·알파·기준선·마젠타 잔여 픽셀을 검사했다.
- Playwright 클라이언트와 실제 브라우저에서 새 초상화 6종, 전투 진입, 배달 라이더 뽑기·필드 배치와 콘솔 무오류를 확인했다.
- 남은 흔함 4종(`fl1`, `fb1`, `hp1`, `hc1`)의 마스터·8프레임 공격·3방향 8프레임 이동을 문서 기준으로 재생성해 흔함 10종 교체를 완료했다.
- 전단지·붕어빵·부하 비둘기·발톱 궤적을 각 공격 프레임에 귀속하고, 동물형도 180px 높이·y=244 기준선으로 인간형과 같은 시각 바운딩 박스에 맞췄다.
- 네 캐릭터의 256px 모션과 512/128/48px UI 에셋을 `public/sprites/common/`에 추가하고 레지스트리를 새 경로로 전환했다.
- `hp1`의 높은 날갯짓에서도 y=244 기준선을 넘지 않도록 생성 스프라이트 정규화기의 큰 프레임 축소 조건을 보정했다.
- `yarn build` 성공. 흔함 전체 70개 에셋·320개 모션 프레임의 크기·알파·기준선·마젠타 잔여 픽셀을 검사했다.
- Playwright 클라이언트 실행 후 인앱 브라우저에서 새 초상화 4종, `fb1` 뽑기·필드 렌더링을 확인했고 콘솔 경고·오류가 없었다.

## TODO

- 안흔함·특별함의 4프레임 및 깨진 버스트를 새 8프레임 규격으로 순차 교체한다.
